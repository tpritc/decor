<div class="max-w-7xl mx-auto">
  <div class="flex justify-between items-center mb-4">
    <h1 class="text-2xl font-semibold"><%= @owner.user_name %>'s Components</h1>
    <% if Current.owner == @owner || admin? %>
      <%= link_to "Add Component", new_owner_component_path(@owner), class: "bg-stone-900 hover:bg-stone-800 text-white text-sm font-medium py-2 px-3 rounded" %>
    <% end %>
  </div>

  <% if @components.any? %>
    <div class="bg-white border border-stone-200 rounded">
      <table class="min-w-full divide-y divide-stone-200">
        <thead class="bg-stone-50">
          <tr>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Type</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Description</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-stone-500 uppercase tracking-wider">Computer</th>
            <th class="px-4 py-2"></th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-stone-200 text-sm">
          <% @components.each do |component| %>
            <tr>
              <td class="px-4 py-3 whitespace-nowrap font-medium text-stone-900"><%= component.component_type.name %></td>
              <td class="px-4 py-3 text-stone-600"><%= truncate(component.description, length: 50) || "—" %></td>
              <td class="px-4 py-3 whitespace-nowrap text-stone-600">
                <% if component.computer %>
                  <%= link_to component.computer.computer_model.name, owner_computer_path(@owner, component.computer), class: "text-stone-700 hover:text-stone-900" %>
                <% else %>
                  <span class="text-stone-400">Spare</span>
                <% end %>
              </td>
              <td class="px-4 py-3 whitespace-nowrap text-right">
                <%= link_to "View", owner_component_path(@owner, component), class: "text-stone-700 hover:text-stone-900" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="text-sm text-stone-500">No components registered yet.</p>
  <% end %>

  <div class="mt-6">
    <%= link_to "← Back to #{@owner.user_name}", @owner, class: "text-sm text-stone-700 hover:text-stone-900" %>
  </div>
</div>
